package corejava;

public class Hello {
	
	/*
	 * ======================Access Modifiers =======================
	 * a> public					: access everywhere		
	 * b> protected					: access in class, same package & child class in different package
	 * c> default(package private)	: access in class & same package
	 * d> private					:access in class only 
	 * 
	 */
	
	// identifiers : names that we give for identification of class, variable, method and such
	// Delimiters : brackets {} and such
	// static keyword	: for efficient memory management. values can be changed. used through class
	//main + ctrl + space
	//main method return type always void coz JVM calls it and even if we return something it does nothing
	static public final void main(String... java) { // ... vary argument 
		
		//sysout + ctrl + space
		System.out.println("Good Morning!");
		
		//F11 shortcut to run program
	}
}
/*
 * ----------------------------------
 * | SN | Name | Age | Phone | City |
 * ----------------------------------
 * | 1  | Ram  | 20  | 4417  | Ktm  |
 * ----------------------------------
 * 
 */
